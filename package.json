{
  "name": "dsh-remote",
  "version": "0.7.0-mod",
  "private": true,
  "dsh": {
    "bundle": {
      "patch": "./packages/plugin/cordis.patch.yml"
    },
    "client": {
      "inject": [
        "@deepseek-ai/dsh-client-runtime",
        "@deepseek-ai/dsh-client-ui-slots"
      ],
      "platform": "web"
    }
  },
  "description": "DSH 移动远程控制台: DSH 插件(内置网关自启停) + 独立网关 + Android App 一体",
  "updateNotes": "0.7.0-mod：修复桌面端侧边栏归档会话开关无效（移除失效拦截逻辑，开关恢复真实切换）；全站去除原作者赞助功能，仓库链接 / 反馈渠道改为 fork 自有（GitHub 仓库 + B 站新地址 + 新增邮箱 p2128887242@outlook.com，删除 gitee 渠道）；新增「设置 → 通用 → prompt 转写」功能（开关 + OpenAI 兼容 API 配置 + 连接/功能测试 + 全屏输入框转写按钮，内置固定 System Prompt 与豆包提示，支持一键复制）；README 重写（中英双语文档同步更新）。",
  "bin": "gateway.js",
  "pkg": {
    "assets": [
      "public/**/*",
      "apk/**/*"
    ],
    "targets": [
      "node22-linux-x64",
      "node22-win-x64"
    ],
    "outputPath": "dist"
  },
  "scripts": {
    "start": "node gateway.js",
    "check": "node --check gateway.js && node --check gateway-stats.cjs && node --check packages/plugin/index.mjs && node --check public/app.js && node --check public/admin.js && node --check public/i18n.js && node --check public/theme.js && node --check public/sha256.js && node --check public/md.js && node --check public/history-core.js && node --check public/transcribe-core.js && node --check public/desktop/desktop.js && node --check public/desktop/i18n.js && node --test tests/stats.test.js tests/gateway.test.js tests/md.test.js tests/network-regression.test.js tests/history.test.js tests/transcribe-core.test.js",
    "test-stats": "node --test tests/stats.test.js",
    "sync-app": "npx cap sync android",
    "build-app": "node scripts/prepare-version.mjs && npx cap sync android && cd android && ./gradlew assembleDebug",
    "build-bin": "pkg .",
    "publish": "node scripts/publish.js && node scripts/sync-plugin.mjs",
    "sync-plugin": "node scripts/sync-plugin.mjs",
    "sync-standalone": "node scripts/sync-standalone.mjs",
    "release": "node scripts/release.mjs"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "@yao-pkg/pkg": "^6.22.0"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/app": "^8.1.1",
    "@capacitor/camera": "^8.2.2",
    "@capacitor/core": "^8.5.0",
    "@capacitor/local-notifications": "^8.3.0"
  }
}
