name: release-build

# 云端构建发版资产: Android APK + Linux/Win 单文件网关, 并上传到 GitHub Release。
# - push tag v*: 自动触发(发版只需打 tag, 不再本地传大文件)
# - workflow_dispatch: 手动触发(使用当前 package.json 版本对应的 vX.Y.Z release)
on:
  workflow_dispatch:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # changelog 需要完整 tag 历史

      - name: Determine release tag
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TAG="v$(node -p "require('./package.json').version")"
          else
            TAG="${{ github.ref_name }}"
          fi
          echo "RELEASE_TAG=${TAG}" >> "$GITHUB_ENV"
          echo "Release tag: ${TAG}"

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Accept SDK licenses
        run: yes | sdkmanager --licenses >/dev/null 2>&1 || true

      - name: Install dependencies
        run: npm ci

      # 必须在任何构建步骤之前验证：Build Android APK 会先跑 prepare-version.mjs
      # 改写 public/update.json（新时间戳/无 sha），publish 又会写入 sha/时间戳；
      # 只有构建前能检查“仓库提交的同步产物与 public/ 一致”。
      - name: Verify plugin sync consistency
        run: |
          npm run sync-plugin
          git diff --exit-code -- packages/plugin

      - name: Build Android APK
        run: npm run build-app

      - name: Copy APK and write update.json
        run: npm run publish

      - name: Build single-file binaries
        run: npm run build-bin

      - name: Normalize binary names
        run: |
          cp dist/dsh-remote-linux dist/dsh-remote-linux-x64
          cp dist/dsh-remote-win.exe dist/dsh-remote-win-x64.exe
          ls -la dist

      - name: Pack plugin tarball (mod fork: 插件以 Releases 内 tgz 交付)
        working-directory: packages/plugin
        run: npm pack

      - name: Publish npm
        # mod fork: 不发布 npm(无 NPM_TOKEN、包名已改为 dsh-remote-mod-plugin),
        # 插件以上方 tgz + GitHub Releases 交付; 若 fork 后续配置了 NPM_TOKEN 才会发布。
        if: (startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch') && secrets.NPM_TOKEN != ''
        working-directory: packages/plugin
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          PKG_VER=$(node -p "require('./package.json').version")
          NPM_TAG=latest
          if [[ "$PKG_VER" == *-* ]]; then NPM_TAG=next; fi
          if npm view "dsh-remote-mod-plugin@$PKG_VER" version >/dev/null 2>&1; then
            echo "npm 已发布 ${PKG_VER}, skip"
          else
            # 两个 tag 触发的发布任务可能同时到达 npm; 若另一任务刚发布完,
            # 把 duplicate version 视为成功, 但其他发布错误仍然失败。
            if npm publish --tag "$NPM_TAG" --access public --//registry.npmjs.org/:_authToken="$NPM_TOKEN"; then
              :
            elif npm view "dsh-remote-mod-plugin@$PKG_VER" version >/dev/null 2>&1; then
              echo "npm 并发任务已发布 ${PKG_VER}, 视为成功"
            else
              exit 1
            fi
          fi

      - name: Sync standalone repo
        # mod fork: 独立仓库同步依赖上游作者部署密钥, fork 一般无此 secret, 缺失时跳过(不影响发版资产)。
        if: (startsWith(github.ref, 'refs/tags/v') || github.event_name == 'workflow_dispatch') && secrets.DSH_RELEASE_DEPLOY_KEY != ''
        env:
          DSH_RELEASE_DEPLOY_KEY: ${{ secrets.DSH_RELEASE_DEPLOY_KEY }}
        run: npm run sync-standalone

      - name: Generate SHA256SUMS
        run: |
          (
            cd apk && sha256sum dsh-remote.apk
            cd ../dist && sha256sum dsh-remote-linux-x64 dsh-remote-win-x64.exe
          ) > SHA256SUMS.txt
          cat SHA256SUMS.txt

      - name: Write release notes
        run: |
          node -e "
            const fs = require('fs');
            const cp = require('child_process');
            const p = JSON.parse(fs.readFileSync('package.json', 'utf8'));
            let prev = '';
            try { prev = cp.execSync('git tag --sort=-v:refname | head -n 2 | tail -n 1', { encoding: 'utf8' }).trim(); } catch {}
            let log = '';
            if (prev && prev !== 'v' + p.version) {
              try { log = cp.execSync('git log --no-merges --pretty=format:\'- %s\' ' + prev + '..HEAD', { encoding: 'utf8' }).trim(); } catch {}
            }
            log = (log || '').split('\n').filter(l => l && !/^- release: v/.test(l)).join('\n');
            const sums = fs.readFileSync('SHA256SUMS.txt', 'utf8').trim();
            const body = '# DSH Remote v' + p.version + '\n\n' + (p.updateNotes || '') +
              '\n\n## 本次更新\n' + (log || '- 重新构建/发布 v' + p.version) +
              '\n\n## SHA-256 校验\n\`\`\`\n' + sums + '\n\`\`\`\n\n## 资产\n' +
              '- \`dsh-remote.apk\` Android App\n' +
              '- \`dsh-remote-linux-x64\` / \`dsh-remote-win-x64.exe\` 免 Node 单文件网关\n' +
              '- \`SHA256SUMS.txt\` 全部资产校验和\n';
            fs.writeFileSync('RELEASE_NOTES.md', body);
          "

      - name: Upload assets to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          draft: false
          body_path: RELEASE_NOTES.md
          files: |
            apk/dsh-remote.apk
            dist/dsh-remote-linux-x64
            dist/dsh-remote-win-x64.exe
            packages/plugin/*.tgz
            SHA256SUMS.txt
